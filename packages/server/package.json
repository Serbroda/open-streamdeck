{
  "name": "open-streamdeck-server",
  "version": "0.0.1",
  "description": "Streamdeck Server",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "start": "npm run build && node dist/index.js --start-server"
  },
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.11",
    "@types/node": "^15.0.1",
    "cross-env": "^7.0.3",
    "typescript": "^4.2.4"
  }
}
